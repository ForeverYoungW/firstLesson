
WITH X AS
(
  SELECT DY, MONTH(DY) AS MTH
    FROM (SELECT DATEADD(MM, 1, (GETDATE() - DATEPART(DY, GETDATE())) + 1) AS DY
            FROM T1
         ) TMP1
   UNION ALL
  SELECT DATEADD(DD, 1, DY), MTH
    FROM X
   WHERE MONTH(DATEADD(DD, 1, DY)) = MTH
)
SELECT MAX(DAY(DY))
  FROM X;