CREATE VIEW V AS
SELECT ENAME AS DATA
  FROM EMP
 WHERE DEPTNO = 10
 UNION ALL
SELECT ENAME + ',$' + CAST(SAL AS VARCHAR(50)) AS DATA
  FROM EMP
 WHERE DEPTNO = 20
 UNION ALL
SELECT ENAME + CAST(DEPTNO AS VARCHAR(50)) AS DATA
  FROM EMP
 WHERE DEPTNO = 30;