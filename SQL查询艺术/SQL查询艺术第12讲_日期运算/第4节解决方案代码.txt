SELECT DATEDIFF(MONTH, MIN_HD, MAX_HD) AS MONTHS,
       DATEDIFF(MONTH, MIN_HD, MAX_HD) / 12 AS YEARS
  FROM (SELECT MIN(HIREDATE) AS MIN_HD,
               MAX(HIREDATE) AS MAX_HD
          FROM EMP
       ) AS X