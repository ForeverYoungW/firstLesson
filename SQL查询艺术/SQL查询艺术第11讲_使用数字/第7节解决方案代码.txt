SELECT DISTINCT(D10/TOTAL) * 100 AS PCT 
  FROM (SELECT DEPTNO,
               SUM(SAL) OVER() AS TOTAL,
               SUM(SAL) OVER(PARTITION BY DEPTNO) AS D10
          FROM EMP) X
 WHERE DEPTNO = 10;